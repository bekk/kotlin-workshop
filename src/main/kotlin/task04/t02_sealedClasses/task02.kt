package task04.t02_sealedClasses

/**
 * Write a function handleResult that takes a Result object (from Task 1) as a parameter and returns a string.
 * The function should use a when expression to handle different cases: if the result is Success,
 * return the success message, and if it's Error, return the error message from the Exception.
 *
 * In the main function, call handleResult with a Success object and an Error object and write the result to the console.
 */
fun main() {

}

fun handleResult(result: Result): String {
    return ""
}
